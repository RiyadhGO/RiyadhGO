package sa.edu.yamamh.riyadhgo.data;
//we used enum because it used to represent distances, trip lengths, or step counts in different contexts
public enum MeasureUnit {
    Meter,
    KiloMeter,
    Steps
}
